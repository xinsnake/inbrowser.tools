extends ../_base.pug

block content
    h2 base64 converter
    p.center
        label Plain
        #plain(data-language="plain")
    p.center
        button#encode Encode
        button#decode Decode
        button#clear Clear
    p.center
        label Encoded
        #encoded(data-language="plain")
    script(src="base64.js")